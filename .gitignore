# =========================================================
# OS / Editor
# =========================================================
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*.temp

.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates

# =========================================================
# .NET / C# build output
# =========================================================
bin/
obj/
[Bb]uild/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/

TestResults/
coverage/
coverage.xml
coverage.cobertura.xml
*.trx
*.coverage
*.coveragexml

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
project.lock.json
project.assets.json

# Rider / ReSharper
_ReSharper*/
*.DotSettings.user

# =========================================================
# Frontend / dashboard local files
# =========================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
.cache/

# =========================================================
# Secrets / environment files
# =========================================================
.env
.env.*
*.env

# Allow committed examples/templates
!.env.example
!.env.sample
!.env.template

# Local appsettings that may contain secrets
appsettings.Development.json
appsettings.Production.json
appsettings.Staging.json
appsettings.Local.json
appsettings.*.local.json
appsettings.*.secret.json

# Allow committed appsettings template files
!appsettings.json
!appsettings.example.json
!appsettings.sample.json

# ASP.NET user secrets should never be committed
secrets.json

# =========================================================
# Certificates / keys / credentials
# =========================================================
*.pem
*.key
*.pfx
*.p12
*.crt
*.cer
*.csr
*.jks
*.keystore
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.private
*.secret
credentials.json
service-account*.json
google-credentials*.json

# =========================================================
# Docker local overrides
# =========================================================
docker-compose.override.yml
docker-compose.local.yml
docker-compose.*.local.yml

# Keep sample docker compose files
!docker-compose.example.yml
!docker-compose.sample.yml

# =========================================================
# Database / Redis / local runtime data
# =========================================================
postgres-data/
pgdata/
redis-data/
data/
.local-data/
runtime-data/

*.db
*.sqlite
*.sqlite3
*.bak
*.dump
*.sql.gz
*.sql.zip

# Do not commit local seed files that may contain real keys
seed.local.sql
*.local.sql
*.secret.sql

# But keep migration and example SQL files
!migrations/*.sql
!examples/*.sql

# =========================================================
# Logs
# =========================================================
logs/
log/
*.log
*.log.*
*.err
*.out

# =========================================================
# Publish / deployment artifacts
# =========================================================
publish/
artifacts/
.deploy/
deploy/
*.zip
*.tar
*.tar.gz

# =========================================================
# Repomix / generated AI review bundles
# =========================================================
repomix-output.*
*.repomix.*


# Environment / secrets
.env
.env.*
*.env

# Cho phép commit file mẫu
!.env.example

# Local appsettings có thể chứa secret
appsettings.Development.json
appsettings.Production.json
appsettings.Staging.json
appsettings.Local.json
appsettings.*.local.json

# Cert / key
*.pem
*.key
*.pfx
*.p12
*.crt
*.cer

# Docker local override
docker-compose.override.yml
docker-compose.local.yml

# Local data
postgres-data/
redis-data/
data/